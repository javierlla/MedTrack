article 
    h2(class="appointment-card")= appointment.id
    
    p(class="appointment-card__date")=appointment.date
    p(class="appointment-card__patient")= appointment.Patient.name
    p(class="appointment-card__doctor")= appointment.Doctor.name
    p(class="appointment-card__doctor-spaciallity")= appointment.Doctor.speciallity
    
    a(href=`/apppointment/${appointment.id}/edit`)
        button Edit
    form(action=`/appointment/${appointment.id}/delete`, method="post") 
        button Delete