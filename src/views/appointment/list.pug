extends ../layout

block content 
    h1 Appointments 

    section(class="appointment-list")
        if appointments 
            each appointment in appointments 
                a(href=`/appointment/${appointment.id}`)
                    include appointmentCard
        else 
            p There is no appointment available